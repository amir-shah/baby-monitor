This repo contains a server and application. To run the Flask server:
export FLASK_APP=test.py
flask run --host=0.0.0.0

Your flask app will be running on :5000

To run baby-app: 
npm install
npm start
